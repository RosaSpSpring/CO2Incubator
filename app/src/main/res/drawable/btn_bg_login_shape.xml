<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="false">
        <!---点击状态-->
        <shape android:shape="rectangle">

            <!-- android:radius 弧形的半径 -->
            <corners android:radius="50dip" />
            <!-- padding：Button里面的文字与Button边界的间隔 -->
            <padding
                android:left="10dp"
                android:top="10dp"
                android:right="10dp"
                android:bottom="10dp"
                />
            <!-- 边框的宽度和颜色颜色 -->
            <stroke
                android:width="0dp"
                android:color="#000000" />
            <!-- 背景色 -->
            <solid android:color="#13aae1"/>
        </shape>
    </item>
    <item android:state_pressed="true">
        <!---非点击状态-->
        <!---背景填充颜色-->
        <shape android:shape="rectangle">
            <!-- android:radius 弧形的半径 -->
            <corners android:radius="50dip" />
            <!-- padding：Button里面的文字与Button边界的间隔 -->
            <padding
                android:left="10dp"
                android:top="10dp"
                android:right="10dp"
                android:bottom="10dp"
                />
            <!-- 边框的宽度和颜色颜色 -->
            <stroke
                android:width="0dp"
                android:color="#000000" />
            <!-- 背景色 -->
            <solid android:color="#0d87b4"/>
        </shape>
    </item>
</selector>